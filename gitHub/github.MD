Working Directory => Stage => Local Repository => Remote Repository.

git remote add origin https://github.com/AhmadOrpu/10DaysGitAndGitHubAndMarkDown.git

git branch -M Master

git push -u origin Master



Clone : cloning from other repository to my local repository 
Fork : cloning from other remote repository to my remote repository

//clone
git clone git@github.com:AhmadOrpu/10DaysGitAndGitHubAndMarkDown.git


// push 
git push -u origin Master
git push (gitHub er Master branch er sathe add hobe )
git push origin /branchName (tumi exactly jei branch name e pathate chaw)

//(akhn development branch theke kiso change kore oita push korte gele conflict korbe cz remote e tu development name kono branch nai .
tai command dite hobe) -
git push --set-upstream origin development


//pull
git pull
